pipeline {
    agent any
    stages {
        stage('build') {
            steps{
                sh '''
		echo "build completed."
		echo "this build was triggered automatically"
		'''
            }
        }
    }
}
